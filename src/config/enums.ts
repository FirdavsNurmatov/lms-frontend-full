export enum Gender {
  MALE = "MALE",
  FEMALE = "FEMALE",
}

export enum Roles {
  ADMIN = "ADMIN",
  TEACHER = "TEACHER",
  STUDENT = "STUDENT",
}

export enum Status {
  ACTIVE = "ACTIVE",
}

export enum PaymentType {
  CASH = "CASH",
  CREDIT_CARD = "CREDIT_CARD",
}
